services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: loanmatcher
      POSTGRES_USER: loanmatcher
      POSTGRES_PASSWORD: loanmatcher
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
